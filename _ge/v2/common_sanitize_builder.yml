serializer:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.serializer
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=60
  volumes:
    - serializer_data:/var/lib/data
  restart: unless-stopped
