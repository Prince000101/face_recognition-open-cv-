queue:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.queue
    args:
      NODE_ENV: production
  ports:
    - "3000:3000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=120
  volumes:
    - queue_data:/var/lib/data
  restart: unless-stopped
