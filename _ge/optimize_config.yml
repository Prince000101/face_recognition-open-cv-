modal:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.modal
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=600
  volumes:
    - modal_data:/var/lib/data
  restart: unless-stopped
