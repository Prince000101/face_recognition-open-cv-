dashboard:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.dashboard
    args:
      NODE_ENV: production
  ports:
    - "3000:3000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=600
  volumes:
    - dashboard_data:/var/lib/data
  restart: unless-stopped
