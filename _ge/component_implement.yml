token:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.token
    args:
      NODE_ENV: production
  ports:
    - "3000:3000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=120
  volumes:
    - token_data:/var/lib/data
  restart: unless-stopped
