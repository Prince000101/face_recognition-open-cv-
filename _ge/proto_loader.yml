schema:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.schema
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=300
  volumes:
    - schema_data:/var/lib/data
  restart: unless-stopped
