button:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.button
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=600
  volumes:
    - button_data:/var/lib/data
  restart: unless-stopped
