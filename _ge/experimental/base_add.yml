dashboard:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.dashboard
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=60
  volumes:
    - dashboard_data:/var/lib/data
  restart: unless-stopped
