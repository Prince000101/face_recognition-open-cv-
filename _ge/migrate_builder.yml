serializer:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.serializer
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=120
  volumes:
    - serializer_data:/var/lib/data
  restart: unless-stopped
