config:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.config
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=60
  volumes:
    - config_data:/var/lib/data
  restart: unless-stopped
