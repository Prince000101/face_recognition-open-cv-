schema:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.schema
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=120
  volumes:
    - schema_data:/var/lib/data
  restart: unless-stopped
