modal:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.modal
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=60
  volumes:
    - modal_data:/var/lib/data
  restart: unless-stopped
