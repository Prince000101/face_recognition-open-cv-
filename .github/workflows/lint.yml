api:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.api
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=600
  volumes:
    - api_data:/var/lib/data
  restart: unless-stopped
